# Web agent instructions

- Import data through collaboration artifacts; do not access the Data project directly.
- Keep the dashboard dependency-free and deterministic.
- Verify the generated module with `npm test`.
- Never access another project folder directly.
