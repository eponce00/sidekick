# Comprehensive agent-loop evaluation

- Keep every change inside this project.
- Preserve the `sidekick-comprehensive-eval` sentinel in `src/app.js`.
- Use localized edits for existing files; never replace an existing file wholesale.
- New files may be created with the write tool.
- Inspect `assets/reference.png` with the image tool before changing the interface.
- Verify the page through SideKick's browser at desktop and compact widths.
- Run the requested project check and do not claim completion while it fails.
