# Data agent instructions

- Treat `source/population.csv` as the only source of truth.
- Write derived data under `dist/`.
- Share peer-required files with the collaboration artifact tools.
- Never access another project folder directly.
