# SVG controller evaluation

- Keep the deliverable self-contained in `index.html`; do not add external assets or dependencies.
- Use inline SVG and the exact `data-part` names requested by the user so automated checks can inspect the visual structure.
- Preserve completed controller parts during follow-up revisions.
- Run the requested project check after each turn and do not claim completion when it fails.
